import 'package:flutter/material.dart';

class NutsShopScreen extends StatelessWidget {
  const NutsShopScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('nuts'),
    );
  }
}
