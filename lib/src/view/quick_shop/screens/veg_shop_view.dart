import 'package:flutter/material.dart';

class VegShopScreen extends StatelessWidget {
  const VegShopScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Veg'),
    );
  }
}
